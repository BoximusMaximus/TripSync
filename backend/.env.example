DJANGO_SECRET_KEY=z89vukq(n#(%668&j%93zr_y4e9-d@1i*84ablkqfxx&8u_o2t
DEBUG=True

ALLOWED_HOSTS=backend,localhost,127.0.0.1

POSTGRES_DB=tripsync_db
POSTGRES_USER=tripsync_user
POSTGRES_PASSWORD=tripsync_password
POSTGRES_HOST=db
POSTGRES_PORT=5432

REDIS_URL=redis://redis:6379/0

SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
CSRF_COOKIE_SECURE=False
AUTH_COOKIE_SECURE=False
AUTH_COOKIE_SAMESITE=Lax

#Google Cloud Platform
# GOOGLE MAPS (server key - Geocoding API + Places API (New); IP-restricted in prod; NOT the browser VITE_ key)
GOOGLE_MAPS_SERVER_KEY=

# Browser key for the Maps JavaScript API (HTTP-referrer restricted: http://localhost:5173/*
# and https://tripsync.duckdns.org/*). Baked into the frontend bundle at BUILD time by
# docker-compose.prod.yml (build args) - change it, then `make frontend-rebuild`.
# In dev, put the same value in frontend/.env.local for `npm run dev`.
VITE_GOOGLE_MAPS_API_KEY=

# --- production (EC2): the values above are dev defaults; set these in the real backend/.env ---
# DEBUG=False
# ALLOWED_HOSTS=backend,localhost,127.0.0.1,tripsync.duckdns.org
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# AUTH_COOKIE_SECURE=True
# DJANGO_SECRET_KEY=<a fresh secret WITHOUT '$' - docker compose expands $name inside .env values>
